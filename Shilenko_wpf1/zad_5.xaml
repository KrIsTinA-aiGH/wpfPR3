<Window x:Class="Shilenko_wpf1.zad_5"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Shilenko_wpf1"
        mc:Ignorable="d"
        Title="zad_5" Height="600" Width="1000">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Кнопка Назад (вверху) -->
        <Button Content="Назад" Click="BackButton_Click" 
                Width="80" Height="30" Grid.Row="0" Grid.Column="0" 
                HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,0,0,20"/>

        <!-- Описание задания -->
        <TextBlock TextWrapping="Wrap" FontSize="16" Text="Сгенерировать массив размером M столбцов и N строк, содержащий числа от -10 до 10. Отобразить его пользователю. Отсортировать элементы в нем по возрастанию и по убыванию. Вывести результаты сортировки в двух таблицах. Найти максимальный и минимальный элементы массива." 
                   Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="4" Margin="0,0,0,20"/>

        <!-- Ввод столбцов -->
        <TextBlock Text="Введите количество столбцов:" FontSize="16" 
                   Grid.Row="2" Grid.Column="0" HorizontalAlignment="Right" Margin="0,0,10,10" VerticalAlignment="Center"/>
        <TextBox x:Name="rm" Height="25" Width="80" 
                 Grid.Row="2" Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,0,0,10"/>

        <!-- Ввод строк -->
        <TextBlock Text="Введите количество строк:" FontSize="16" 
                   Grid.Row="2" Grid.Column="2" HorizontalAlignment="Right" Margin="0,0,10,10" VerticalAlignment="Center"/>
        <TextBox x:Name="rn" Height="25" Width="80" 
                 Grid.Row="2" Grid.Column="3" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,0,0,10"/>

        <!-- Кнопка -->
        <Button x:Name="Otvet" Content="Сгенерировать и отсортировать" Height="30" 
                Click="Otvet_Click" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="4" 
                Margin="0,0,0,20" HorizontalAlignment="Center" Width="250"/>

        <!-- Исходный массив -->
        <TextBlock Text="Исходный массив:" FontWeight="Bold" FontSize="16" 
                   Grid.Row="4" Grid.Column="0" HorizontalAlignment="Center" Margin="0,0,0,10"/>
        <TextBox x:Name="OriginalBox" IsReadOnly="True" TextWrapping="Wrap" 
                 Grid.Row="4" Grid.Column="0" Margin="10,28.333,9.667,-0.333" VerticalAlignment="Stretch" Grid.RowSpan="2"/>

        <!-- Сортировка по возрастанию -->
        <TextBlock Text="По возрастанию:" FontWeight="Bold" FontSize="16" 
                   Grid.Row="4" Grid.Column="1" HorizontalAlignment="Center" Margin="0,0,0,10"/>
        <TextBox x:Name="AscBox" IsReadOnly="True" TextWrapping="Wrap" 
                 Grid.Row="4" Grid.Column="1" Margin="10.333,28.333,9.667,-0.333" VerticalAlignment="Stretch" Grid.RowSpan="2"/>

        <!-- Сортировка по убыванию -->
        <TextBlock Text="По убыванию:" FontWeight="Bold" FontSize="16" 
                   Grid.Row="4" Grid.Column="2" HorizontalAlignment="Center" Margin="0,0,0,10"/>
        <TextBox x:Name="DescBox" IsReadOnly="True" TextWrapping="Wrap" 
                 Grid.Row="4" Grid.Column="2" Margin="10.333,28.333,10.333,-0.333" VerticalAlignment="Stretch" Grid.RowSpan="2"/>

        <!-- Результаты -->
        <TextBlock Text="Результаты:" FontWeight="Bold" FontSize="16" 
                   Grid.Row="4" Grid.Column="3" HorizontalAlignment="Center" Margin="0,0,0,10"/>
        <TextBox x:Name="ResultBox" IsReadOnly="True" TextWrapping="Wrap" 
                 Grid.Row="4" Grid.Column="3" Margin="9.667,28.333,10.333,-0.333" VerticalAlignment="Stretch" Grid.RowSpan="2"/>
    </Grid>
</Window>